{% extends 'base.html.twig' %}

{% block title %}
	{{ parent() }} {{ car.name }} {# parent() will display the title from base view "AutoTrader" #}
{% endblock %}

{% block body %}
	<h1><b>{{ car.name }}</b></h1>
	<h3>Make: {{ car.make }}</h3>
	<p>{{ car.description }}</p>
	<a href="{{ url('offer') }}">Back</a>
{% endblock %}